<h1>
  VMS connector information
  <a href="<%=edit_vms_connector_path(@vms_connector)%>" class="btn btn-warning"><span class="glyphicon glyphicon-pencil"></span> Edit</a>
</h1>

<div class="media">
  <table class="table table-hover">
    <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th> </th>
    </tr>
    </thead>

    <tbody>
        <tr>
          <td><%= @vms_connector.name %></td>
          <td><%= @vms_connector.description %></td>
          <td>
            <% if(@vms_connector.verified?) %>
                    <span class="label label-success">
                    <span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>
                    Verified
                  </span>
            <% else %>
                    <span class="label label-danger">
                    <span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span>
                    Invalid
                  </span>
            <% end %>
          </td>
        </tr>
    </tbody>
  </table>
</div>