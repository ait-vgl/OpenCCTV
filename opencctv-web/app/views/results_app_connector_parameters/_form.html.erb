<%= form_for([@results_app_connector,@results_app_connector_parameter]) do |f| %>
  <% if @results_app_connector_parameter.errors.any? %>
    <div id="error_explanation", class="alert alert-danger" role="alert">
      <h2><%= pluralize(@results_app_connector_parameter.errors.count, "error") %> prohibited this results_app_connector_parameter from being saved:</h2>

      <ul>
      <% @results_app_connector_parameter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <!--Form fields-->
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :required %><br>
    <%= f.check_box :required %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.hidden_field :editable, value: true %>
  </div>

  <!--Submit buttons-->
  <div class="actions">
    <%= f.submit class:'btn btn-success btn-mini' %>
    <%= link_to 'Cancel', @results_app_connector, class: 'btn btn-danger' %>
  </div>
<% end %><!--Form ends-->
