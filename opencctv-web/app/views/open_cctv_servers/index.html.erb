<%= render :template => "open_cctv_servers/show"%>

<!-- The Old template for index page - REMOVE LATER -->
<!--
<div class="container">
  <ol class="breadcrumb">
    <li><# link_to 'Home', root_path %></li>
    <li class="active">OpenCCTV Servers</li>
  </ol>
</div>



<div class="container">
  <h1>
    All registered OpenCCTV servers
    <a href="<%#new_open_cctv_server_path %>" class="btn btn-success">
      <span class="glyphicon glyphicon-upload"></span>
      Register a New OpenCCTV Server
    </a>
  </h1>

  <%# if @open_cctv_servers.empty? %>
      <p>
      <div class="alert alert-warning">
        Currently no OpenCCTV servers are registered with the system.
      </div>
      </p>
  <%# else %>
      <div class="media">
        <table class="table table-hover">
          <thead>
          <tr>
            <th>Name</th>
            <th>IP</th>
            <th>Port</th>
            <th>Status</th>
            <th>Process ID</th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <%# @open_cctv_servers.each do |opencctv_server| %>
              <tr>
                <td><%# opencctv_server.name %></td>
                <td><%# opencctv_server.host %></td>
                <td><%# opencctv_server.port %></td>
                <%# if opencctv_server.status.blank? %>
                    <td><%# 'Unknown' %></td>
                <%# else %>
                    <td><%# opencctv_server.status %></td>
                <%# end %>
                <%# if opencctv_server.pid.blank? %>
                    <td><%# 'Unknown' %></td>
                <%# else %>
                    <td><%# opencctv_server.pid %></td>
                <#% end %>
                <td><%# link_to 'Show', opencctv_server , :class => "btn btn-xs btn-default" %></td>
                &lt;!&ndash; TODO Allow editing and destroying only if the OpenCCTV server is not running &ndash;&gt;
                <td><%# link_to 'Edit', edit_open_cctv_server_path(opencctv_server), :class => "btn btn-xs btn-warning" %></td>
                <td><%# link_to 'Destroy', opencctv_server, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-xs btn-danger", data: {confirm: 'Are you sure?'} %></td>
              </tr>
          <%# end %>
          </tbody>
        </table>
      </div>
  <%# end %>
</div>-->
