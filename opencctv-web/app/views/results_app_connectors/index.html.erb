<div class="container">
  <ol class="breadcrumb">
    <li><%= link_to 'Home', root_path %></li>
    <li class="active">Results apllication connectors</li>
  </ol>
</div>

<div class="container">
  <h1>
    All uploaded results apllication connectors
    <a href="<%= new_results_app_connector_path %>" class="btn btn-success"> <span class="glyphicon glyphicon-upload"></span>
      Upload a new results apllication connector</a>
  </h1>

  <% if @results_app_connectors.empty? %>
      <p>

      <div class="alert alert-warning">
        Currently no results apllication connectors are uploaded to the system.
      </div>
      </p>
  <% else %>
      <div class="media">
        <table class="table table-hover">
          <thead>
          <tr>
            <th>#</th>
            <th>Name</th>
            <th>Description</th>
            <th></th>
            <th colspan="3"></th>
          </tr>
          </thead>

          <tbody>
          <% i = 1 %>
          <% @results_app_connectors.each do |results_app_conn| %>
              <tr>
                <td><%= i %></td>
                <td><%= results_app_conn.name %></td>
                <td><%= results_app_conn.description %></td>
                <td>
                  <% if (results_app_conn.verified?) %>
                    <span class="label label-success">
                    <span class="glyphicon glyphicon-ok-sign" aria-hidden="true"></span>
                    Verified
                  </span>
                  <% else %>
                    <span class="label label-danger">
                    <span class="glyphicon glyphicon-remove-sign" aria-hidden="true"></span>
                    Invalid
                  </span>
                  <% end %>
                </td>
                <td><%= link_to 'Show', results_app_conn, :class => "btn btn-xs btn-default" %></td>
                <td><%= link_to 'Edit', edit_results_app_connector_path(results_app_conn), :class => "btn btn-xs btn-warning" %></td>
                <td><%= link_to 'Destroy', results_app_conn, method: :delete, :class => "btn btn-xs btn-danger", data: {confirm: 'Are you sure?'} %></td>
              </tr>
              <% i = i + 1 %>
          <% end %>
          </tbody>
        </table>
      </div>
  <% end %>
</div>

